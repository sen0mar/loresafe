# UI Reference Images

Current reference:

```txt
context/images/dashboard/threadsync-dashboard-dark-reference.png
```

Use this image for layout, density, color, and component direction. The visible old app name in the image is not current; the product name is ThreadSync.

Add future references here only after the user confirms the direction:

```txt
context/images/dashboard/
context/images/homepage/
```
